# @callsitehq/runtime

Runtime dispatch engine for Callsite capabilities.

Use this package to create a web-standard `Request -> Response` handler from a
set of Callsite capabilities.

```ts
import { createFetchHandler } from "@callsitehq/runtime";
```

## Status

Early `0.x` package. Runtime protocol handling is still being expanded.
